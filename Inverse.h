#include "Matrix.h"
#include "Determinant.h"

#ifndef _Inverse_
#define _Inverse_
matrix Inverse(matrix T);
//this function indicates to calculate the inverse matrix of a matrix if it has one .
//matrix T must be square and there has one strategy to make sure.
//algorithm is similiar to that of the calculation of Determinants
//when calculating, using row transformation. Not column, so row related functions are called.

#endif
